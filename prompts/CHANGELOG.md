# Prompt Changelog

All changes that affect prompt behaviour or placeholders must be recorded here.

## [Unreleased]

### Added
- `rag_system.txt` — system prompt for the RAG chain; requires `{context}` placeholder.
- `summary_system.txt` — system prompt for the summarization chain; requires `{text}` placeholder.

